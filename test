	add some content to test clone
	
	just cloning any git repository, next time onwards after pulling
	changes, the gitk is showing
	at the top "Local uncommitted changes, not checked in to index". Why
	it is happening.

